# praxi

## Overview

Anomaly detection for correlated time-series data. Implements the algorithm of .

## Installation

praxi can be installed by running the following code in an R session:

```r
if(system.file(package = "praxi") != ""){
  remove.packages("praxi")
}
library("remotes")
install_github("DylanBahia/praxi",force=TRUE)
```

## Examples

Examples can be found in .
